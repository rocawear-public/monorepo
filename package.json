{
  "name": "@habbo-extension/root",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build -workspaces --if-present",
    "start": "npm run start -workspace apps/server --if-present",
    "dev": "",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "./apps/*",
    "./libs/*"
  ],
  "devDependencies": {
    "typescript": "^4.9.4"
  }
}
